const init = require('./lib/init')
const start = require('./lib/start')
const stop = require('./lib/stop')
const selfTest = require('./lib/self-test')

module.exports = {
  init,
  start,
  stop,
  selfTest
}
